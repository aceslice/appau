---
import PG from "../components/PG.astro";
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import featuredLinks from "../libs/featuredLinks";
import Link from "../components/Link.astro";
import AdsLink from "../components/AdsLink.astro";
import Button from "../components/Button";
import UsernameTag from "../components/UsernameTag.astro";
const greetings = [
  "Akwaaba!",
  "こんにちは",
  "안녕하세요",
  "你好",
  "Здравствуйте!",
  "いらっしゃいませ",
];

// Initialize with a random greeting
let randomGreeting = greetings[Math.floor(Math.random() * greetings.length)];
---

<Layout title="Home">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-8">
    <Header />

    <h1 class="text-4xl sm:text-5xl md:text-6xl font-bold mb-4">
      <span class="text-xl sm:text-2xl text-[#333]">
        <style>
          #greeting {
            transition: opacity 0.5s ease;
          }
          #greeting.fade {
            opacity: 0;
          }
        </style>
        <span id="greeting">{randomGreeting}</span>
      </span>
      <br /> I'm Gideon Appau.
    </h1>
    <p
      class="text-base md:text-sm font-medium mb-4 text-gray-500 md:w-3/4"
      style="line-height: 2"
    >
      Hey there, I'm not just a coder—I’m someone who loves turning ideas into
      real experiences. Every line of code is part of my story, crafted over
      late nights filled with coffee and breakthroughs. Check out <a
        href="about"
      >
        <span
          class="font-semibold text-green-700"
          style="border-bottom: 1px dashed currentColor; padding: 0 0.2em;"
          >my story</span
        ></a
      > to see where it all started, or head over to <a href="projects">
        <span
          class="font-semibold text-orange-700"
          style="border-bottom: 1px dashed currentColor; padding: 0 0.2em;"
          >what I'm creating</span
        ></a
      > right now. And if you’re up for a friendly chat or some idea sharing, join
      me on Discord!
    </p>

    <p
      class="italic text-sm md:text-sm font-medium text-gray-500 md:w-3/4 mb-8"
    >
      You can just call me
      <span
        class="text-indigo-600"
        style="border-bottom: 1px dashed currentColor; padding: 0 0.2em;"
      >
        Ace
      </span>
      — a name that inspires me to live boldly and embrace mastery in every endeavor.
    </p>

    <PG />
    <AdsLink
      backgroundColor="#582FFF"
      textColor="#fff"
      url="https://knowva.com"
      description="— Knowva finds you your next mentor"
      icons={[
        "i-lucide-rocket",
        "i-lucide-message-circle",
        "i-lucide-calendar-range",
        "i-lucide-library-square",
        "i-lucide-sparkle",
        "i-lucide-users-round",
        "i-lucide-shopping-bag",
      ]}
    />
    <AdsLink
      backgroundColor="#2BCC5A90"
      description="— Market Mate: local gateway to fresh produce & best deals."
      icons={[
        "i-lucide-apple",
        "i-lucide-salad",
        "i-lucide-shopping-bag",
        "i-lucide-handshake",
        "i-lucide-laugh",
      ]}
      url="https://marketmate.example"
      textColor="#364153"
    />
    <!-- <h2
    class="mb-4 flex items-center gap-2 font-bold max-sm:mt-8"
    id="featured">
    <div class="book-heart inline-block opacity-60"></div> Featured
  </h2> -->
    <Button client:idle />
  </div>
</Layout>
<Footer />
<script>
  const greetings = [
    "Akwaaba!",
    "こんにちは",
    "안녕하세요",
    "你好",
    "Здравствуйте",
    "いらっしゃいませ",
  ];

  // Function to fade out, change text, then fade in
  function changeGreeting() {
    const el = document.getElementById("greeting");
    if (el) {
      el.classList.add("fade");
      setTimeout(() => {
        el.textContent =
          greetings[Math.floor(Math.random() * greetings.length)];
        el.classList.remove("fade");
      }, 500); // Fade-out duration reduced for snappier UX
    }
  }

  // Change greeting every 10 seconds
  setInterval(changeGreeting, 10000);
</script>
